# Required only for the diagnostic-gated DMA-BUF import path used by
# TBSTREAM_ZC_IMPORT (zero-copy patches 14-15).  Keep this disabled unless the
# workload explicitly uses imported GPU-owned NHI frame pools.
options thunderbolt_stream zc_diagnostic_dmabuf=1
